#!/usr/bin/env node

const Server = require("../lib/server")
const Sessions = require("../lib/sessions")

let server = new Server(new Sessions())

let port = parseInt(process.argv[2], 10)
let host = "localhost"
server.listen(port, host)
